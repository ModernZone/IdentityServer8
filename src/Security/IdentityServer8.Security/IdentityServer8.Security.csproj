<Project Sdk="Microsoft.NET.Sdk">

    <!--
      Vendored from https://github.com/alexhiggins732/IdentityServer8, pinned commit 08051c9b.
      See ../../VENDORED_COMMIT.txt for provenance. This csproj is a minimal replacement
      authored for this solution (see IdentityServer8.Storage.csproj comment for rationale).

      Microsoft.AspNetCore.Http.Abstractions / Mvc.Abstractions are part of the
      Microsoft.AspNetCore.App shared framework on net10.0 (no NuGet package needed/available
      at this version) - use FrameworkReference instead of the upstream repo's 2.2.0-era
      PackageReferences.
    -->

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App" />
    </ItemGroup>

</Project>
